#import "../Fragments/PaymentMethodFragment.graphql"

mutation CreditCard($input: AddCreditCardInput!) {
    addCreditCard(input: $input) {
        success
        message
        paymentMethod {
            ...PaymentMethodFragment
        }
    }
}
