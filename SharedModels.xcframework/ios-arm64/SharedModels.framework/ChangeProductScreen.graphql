#import "../Fragments/CatalogProductFragment.graphql"

query ChangeProductScreen($lineId: String!) {
    changeProductScreen(lineId: $lineId) {
        nextRenewalDate
        catalogProducts {
            ...CatalogProductFragment
        }
    }
}
