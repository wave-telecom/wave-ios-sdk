#import "../Fragments/CatalogProductFragment.graphql"

query PurchaseTopupScreen($lineId: String!) {
    purchaseTopupScreen(lineId: $lineId) {
        currentTopups {
            unitType
            unitAvailableAmount
        }
        catalogProducts {
            ...CatalogProductFragment
        }
    }
}
