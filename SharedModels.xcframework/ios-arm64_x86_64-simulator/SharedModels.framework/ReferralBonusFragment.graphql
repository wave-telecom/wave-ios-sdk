#import "ReferralProductFragment.graphql"

fragment ReferralBonusFragment on ReferralBonus {
    totalDataInGibibytes
    redeemedProducts {
        ...ReferralProductFragment
    }
    unredeemedProducts {
        ...ReferralProductFragment
    }
}
