#import "Fragments/LineFragment.graphql"

mutation ActivateLine($activateLineInput: ActivateLineInput!) {
    activateLine(input: $activateLineInput) {
        success
        message
        line {
            ...LineFragment
        }
   }
}
